# Local NCTL runtime (keys, binaries, storage, logs)
assets/**
!assets/.gitkeep

*.log
.env
.env.*

# Personal local helper (not part of the published repo)
push_to_docker.sh
.github_credentials
# Local Cursor config (mcp.json, etc.); keep shared agent rules tracked
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.cursor/rules/.gitkeep
